#pragma once

#include "Vector.h"

float interp(float valueStart, float valueEnd, float t);
float dotProduct(const Vector2f& v1, const Vector2f& v2);
float dotProduct(const Vector2f& v1, float v2x, float v2y);

